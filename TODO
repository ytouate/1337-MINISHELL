///tomorrow's todod \\\\

finish the exit_status code;
start handling signals;

========================================
// cases to handle //

signals inside the pipe;
exit status inside the pipe;
ls | rdsafj  | ls | afjak -> 127 >> TODO;
ls | kdjf -> 127;
<< k ls | cat | kdsjkljf -> 127;
ls | ./Makefile -> 0 should be -> 126;
